{
  "version": 1,
  "hooks": {
    "sessionEnd": [
      {
        "type": "command",
        "powershell": "telex --json copilot session-end",
        "bash": "telex --json copilot session-end",
        "timeoutSec": 30
      }
    ],
    "agentStop": [
      {
        "type": "command",
        "powershell": "telex --json copilot turn-guard",
        "bash": "telex --json copilot turn-guard",
        "timeoutSec": 30
      },
      {
        "type": "command",
        "powershell": "$neutral = '{}'; $block = '{\"decision\":\"block\",\"reason\":\"Telex plugin/binary version skew: the telex binary resolved from PATH could not run `copilot drain`. Run `telex copilot drain --help` and `telex --json version`, then use `Get-Command telex` on Windows or `command -v telex` on POSIX to identify the PATH winner. Reinstall a matched plugin/binary release through the versioned installer, ensure its bin directory precedes stale shims such as cargo-installed copies, and restart Copilot. If intentionally rolling back the binary, roll back the plugin to the same release first. `TELEX_COPILOT_DRAIN=off` is only a temporary escape hatch.\"}'; $setting = $env:TELEX_COPILOT_DRAIN; if ($null -ne $setting -and $setting.Trim().ToLowerInvariant() -in @('off', '0', 'false')) { $neutral } else { try { $payload = [Console]::In.ReadToEnd(); $payload | & telex --json copilot drain > $null 2> $null; $code = $LASTEXITCODE } catch { $code = 1 }; if ($code -eq 0) { $neutral } else { $block } }",
        "bash": "if [ -n \"${COPILOT_PLUGIN_ROOT:-}\" ] && [ -f \"$COPILOT_PLUGIN_ROOT/drain-hook.sh\" ]; then sh \"$COPILOT_PLUGIN_ROOT/drain-hook.sh\"; else printf '%s\\n' '{\"decision\":\"block\",\"reason\":\"Telex plugin installation is incomplete: the drain hook launcher is unavailable. Reinstall the telex plugin from a matched release, then restart Copilot.\"}'; fi",
        "timeoutSec": 30
      }
    ]
  }
}
