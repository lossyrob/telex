You are a TEST PEER in a controlled experiment to prove the telex copilot bridge-push mechanism (cross-session message injection). Another Copilot session will push a message to you. Do exactly the following steps, in order, then WAIT. Do not exit.

STEP 1 - Copy the bridge extension into THIS session's extension directory. Run this PowerShell exactly:

$sid = $env:COPILOT_AGENT_SESSION_ID
$dst = Join-Path $HOME ".copilot\session-state\$sid\extensions\telex-bridge"
New-Item -ItemType Directory -Force -Path $dst | Out-Null
Copy-Item "C:\Users\robemanuele\proj\telex\telex-uiserver-push\copilot-bridge\extension.mjs" (Join-Path $dst "extension.mjs") -Force
Write-Output "PEER bridge copied for session $sid"

STEP 2 - Activate the bridge by calling the extensions_reload tool (no arguments). It should report that an extension named "telex-bridge" is running.

STEP 3 - Call the telex_bridge_info tool (no arguments). Print its JSON. Then state clearly: "PEER READY, session id = <your COPILOT_AGENT_SESSION_ID>, endpoint = <the pipe path>".

STEP 4 - WAIT for an incoming message. You will receive a pushed turn labeled "[telex] ...". When you do, follow its instruction precisely (it will ask you to write a small proof file). After writing the file, print "PEER RECEIVED PUSH" and the exact content you wrote, then keep waiting. Do not exit the session.

Begin now with STEP 1.
