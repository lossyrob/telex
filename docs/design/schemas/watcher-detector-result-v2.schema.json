{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://telex.dev/schema/watcher-detector-result-v2.schema.json",
  "title": "Telex Watcher detector result v2",
  "$comment": "Opaque JSON values must be valid I-JSON and RFC 8785 canonicalizable. Runtime-enforced UTF-8 byte limits supplement this schema. Event identity and routing are runtime-owned and are not valid detector fields.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "outcome"
  ],
  "properties": {
    "schemaVersion": {
      "const": 2
    },
    "outcome": {
      "enum": [
        "idle",
        "event",
        "terminal",
        "degraded"
      ]
    },
    "nextState": {
      "$comment": "For event-producing event or terminal results, omission commits the unchanged prior state after durable acceptance."
    },
    "event": {
      "$ref": "#/$defs/event"
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "outcome": {
            "const": "idle"
          }
        },
        "required": [
          "outcome"
        ]
      },
      "then": {
        "not": {
          "required": [
            "event"
          ]
        }
      }
    },
    {
      "if": {
        "properties": {
          "outcome": {
            "const": "event"
          }
        },
        "required": [
          "outcome"
        ]
      },
      "then": {
        "required": [
          "event"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "outcome": {
            "const": "degraded"
          }
        },
        "required": [
          "outcome"
        ]
      },
      "then": {
        "not": {
          "anyOf": [
            {
              "required": [
                "event"
              ]
            },
            {
              "required": [
                "nextState"
              ]
            }
          ]
        }
      }
    }
  ],
  "$defs": {
    "nonControlString": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[^\\u0000-\\u001f\\u007f]+$"
    },
    "event": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "subject",
        "body"
      ],
      "properties": {
        "kind": {
          "$ref": "#/$defs/nonControlString"
        },
        "subject": {
          "$ref": "#/$defs/nonControlString"
        },
        "body": {
          "type": "string"
        },
        "metadata": {
          "type": "object"
        }
      }
    }
  }
}
