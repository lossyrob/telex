# Detector templates are shipped as exact bytes and their manifests pin script
# digests, so checkout conversion must never vary by platform.
telex-watcher/templates/** text eol=lf

# Shell scripts must stay LF because they execute in Unix-like environments.
*.sh text eol=lf
